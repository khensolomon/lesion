import { Extension } from "resource:///org/gnome/shell/extensions/extension.js";

// Empty extension file as requested
export default class LesionExtension extends Extension {
    enable() {}
    disable() {}
}